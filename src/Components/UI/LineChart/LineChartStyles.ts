import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  chartWrapper: {
    height: 100,
    justifyContent: 'flex-start',
  },
  chart: {
    fontSize: 25,
    textAlign: 'center',
  },
  chartDot: {
    borderWidth: 1,
    borderColor: 'white',
  },
})
